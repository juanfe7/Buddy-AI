import { Text, View } from 'react-native';

export default function Page() { // <--- El "export default" es la clave
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Pantalla en construcción</Text>
    </View>
  );
}